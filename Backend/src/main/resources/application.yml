server:
  port: 8080

spring:
  application:
    name: attendance-tracker
  
  config:
    import: optional:file:.env[.properties]
  
  datasource:
    url: jdbc:mysql://localhost:3306/attendance_tracker?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: asdfg1122
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    open-in-view: false

  security:
    user:
      name: admin
      password: admin123

# JWT Configuration
jwt:
  secret: mySecretKey123456789012345678901234567890
  expiration: 86400000 # 24 hours in milliseconds

# CORS Configuration
cors:
  allowed-origins: http://localhost:3000,http://localhost:5173,http://localhost:5175
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  allow-credentials: true

# Admin Configuration
admin:
  signup:
    secret: ${ADMIN_SIGNUP_SECRET:}

logging:
  level:
    com.university.attendance: DEBUG
    org.springframework.security: DEBUG
